# Copy to /etc/fail2ban/jail.d/ on the VPS and adjust logpath.
# If nginx runs in Docker, mount logs to host or use:
#   logpath = /var/lib/docker/volumes/<project>_nginx_logs/_data/access.log
# Or: docker logs --since ... (not ideal for fail2ban; prefer file mount).
#
# [study-archive]
# enabled = true
# filter = nginx-study-auth
# logpath = /var/log/nginx/access.log
# maxretry = 8
# findtime = 600
# bantime = 3600
# action = %(action_)s
