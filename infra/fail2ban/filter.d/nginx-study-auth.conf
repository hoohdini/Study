# Fail2ban filter: repeated failed login attempts (proxy to Next.js returns 401/429)
# Requires nginx access_log to include status; see nginx default.conf access_log.
[Definition]
failregex = ^<HOST> -.*"(GET|POST) /api/auth/login HTTP.*" (401|429)\s
ignoreregex =
